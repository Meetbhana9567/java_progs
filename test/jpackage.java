/* 

package : it's the collection of class and interfaces of the same type
	it actually depicts a folder in the project directory
	it has specified access protection mechanism as "public | private | protected | default"
	it once created can be imported 
		for improting the whole package : "import package-name.*;"
		for specific class : "import package-name.class-name;"
		
*/

import package1.*;

public class jpackage { 

 public static void main(String a[])
 {
 
 person p = new plumber();
 p.showwork();
 
 }
 
}
